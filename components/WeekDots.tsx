/**
 * Removed in v3.0 — streaks and weekly activity were cut.
 * Kept as a render-nothing component so any straggling JSX still compiles.
 */
export default function WeekDots(): null {
  return null;
}