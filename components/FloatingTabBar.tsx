/**
 * Removed in v3.0 — the app is single-screen so a tab bar is unnecessary.
 * Kept as a render-nothing component for back-compat.
 */
export default function FloatingTabBar(): null {
  return null;
}