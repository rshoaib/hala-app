/**
 * Removed in v3.0 — mission/lesson path is no longer part of the app.
 * The app's only data source is `data/phrases.ts`.
 *
 * This file is kept as an empty stub so any straggling imports from
 * legacy route files compile to a no-op rather than a hard error.
 */
export {};